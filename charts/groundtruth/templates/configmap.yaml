apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "groundtruth.fullname" . }}
  labels:
    {{- include "groundtruth.labels" . | nindent 4 }}
data:
  config.yaml: |
    # Managed by the groundtruth Helm chart — edit via `.Values.config`.
    {{- toYaml .Values.config | nindent 4 }}
